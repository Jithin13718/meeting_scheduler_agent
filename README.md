# meeting_scheduler_agent
# The below given instruction need to be followed for using the meeting_Scheduler_Agent
  git clone <>
 cd meeting_scheduler_Agent
 pip install -r requirements.txt
 uvicorn app.main:app --reload
